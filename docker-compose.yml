services:
  shl-api:
    build: .
    ports:
      - "10000:10000"
    environment:
      - API_HOST=0.0.0.0
      - API_PORT=10000
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - ./data:/app/data
    restart: unless-stopped
